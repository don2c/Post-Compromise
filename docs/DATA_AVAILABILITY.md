# Data availability

The generated datasets and scripts used for the reported evaluation are included
in this artifact under `raw_data/` and `scripts/`. The trace is synthetic. It is
not an observed hospital access log.
